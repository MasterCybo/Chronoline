=================================================
КЛИЕНТ
=================================================

Общие:
	- Упростить иконки. Уменьшить их в два раза.
	- Добавить вверху экрана узкое неперемещаемое поле, где будут даваться названия отображаемых объектов (сущностей). 
		Цифровой диапазон отображаемых дат уменьшить в размерах и поместить в этом поле над линейками с датами.
	- Все кнопки убрать с экрана, сделать, чтобы они появлялись при подведении курсора к полю у правого края экрана.
	- Меню сделать на русском языке (а лучше предусмотреть возможность многоязычного интерфейса)
	
Главный экран:
	- При нажатии кнопки Выбора сущностей происходит переход к начальному экрану их выбора, но там не отображаются уже выбранные и 
		нет кнопки вернуться к хроноленте без выбора новых сущностей. Как добавить новую сущность к уже выбранным и построенным?
	- В списках событий указать даты
	- Прокрутка списков колесом мыши
	
Хронолента:
???	- Добавить еле заметную масштабную сетка на рабочем столе
???	- Скрытие/отображение сущностей
	- Линию сущности в два раза толще
	- Обозначить рождение и смерть сущности соответственно: треугольник вверх и треугольник вниз, прям на линии сущности
	- Двойной клик по сущности - установить даты в диапазоне рождение и смерть данной сущности
	- Перетаскивать картинку не только с помощью линейки, но и если захватываешь пустую (белую) область. Вверх - вниз, влево вправо(если возможно)
	- Когда выбираешь только один объект для демонстрации - он отображается полоской у левого края, что не красиво, надо его располагать по центру.
	- Непонятно, что значат горизонтальные линии между полосками сущностей?
	- Редактирование и удаление не работают
	- Если сущность не имеет событий вообще - выводить такую сущность во весь временной диапазон

Линейки:
	- Плавная прокрутка вспомогательной шкалы
	- Инвертировать дату. Сделать наоборот не снизу вверх даты, сверху вниз (внизу младшая дата, вверху старшая)
	- Уменьшить ширину глобальной линейки (самой левой). Сделать, чтобы градации на этой линейке имели не произвольный характер, 
		а соответствовали "круглым" цифрам дат (100, 50, 25, 10, 5,.. лет).
	- Сделать фон (цвет) локальной линейки (правой) отличным от фона глобальной линейки.
	- М.б. сделать индикацию года на локальной линейке не на центре года а на начальном разделителе.
	- При помещении курсора на локальную линейку возникает оранжевая "линия текущего момента" и подсказка с датой - это хорошо. 
		Однако, когда перемещаешь эту линию, то в подсказке момент смены года не совпадает с разделителем года на линейке!
	- Для малых диапазонов дат отображаемых на экране (1-10 лет) желательно чтобы в подсказке отображались не только года, но и месяцы. 
		(при микро диапазонах м.б и числа месяца - продумать)
	- Необходимо предусмотреть переход в "микро" режим - отображение информации на экране, соотв. одному году, полгода, месяц, 10 дней, день. 
		Перемещение ограничителей на глобальной линейке пока не дает такой возможности.

В Админке
1. Добавить пресеты для задания прав пользователей: студент, преподаватель и т.п.

=================================================
СЕРВЕР
=================================================
1. Для первого экрана, добавить крайние даты в сущности для команды partitionentities.php?type=...
2. При сохранении пресета, необходимо вводить его имя, которое потом будет отображаться в списке пресетов